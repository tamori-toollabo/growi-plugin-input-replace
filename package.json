{
  "name": "growi-plugin-input-replace",
  "version": "0.0.1",
  "description": "A Growi plugin that dynamically replaces code placeholders with user inputs./Growiページのコードブロック内の変数を、入力欄の値で動的に置換するプラグイン",
  "type": "module",
  "keywords": [
    "growi",
    "growi-plugin"
  ],
  "devDependencies": {
    "@growi/pluginkit": "^1.1.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^5.2.2",
    "vite": "^7.1.2"
  },
  "growiPlugin": {
    "schemaVersion": "4",
    "types": [
      "script"
    ]
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build"
  }
}
